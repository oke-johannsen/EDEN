import React from "react";

const Notes = ({}) => {
  return (
    <div
      style={{
        borderRadius: "2rem 0 0 2rem",
        height: "100%",
        width: "100%",
        padding: "2rem 0 0 2rem",
        background: "#edd3bd",
      }}
    >
      notes
    </div>
  );
};

export default Notes;
